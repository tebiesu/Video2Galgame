# 开发日志

## 2026-02-22

### 本轮目标
- 优化工作台/沉浸模式的页面结构与交互一致性。
- 修复设置中心、历史任务、语音与滚动体验问题。
- 强化 GalGame 与摘要模式的分流能力，并减少信息噪声。

### 已完成改动

#### 1. 结构与流程
- 工作台新增“总结模式”分流能力：`模板总结` / `角色总结`。
- 解析提交时支持携带 `summaryMode`。
- 历史页改造成“一级任务 + 二级模式选择”流程（选择任务后再进入 GalGame 或摘要模式）。
- 模式页与主工作台继续解耦，支持独立沉浸页渲染。

#### 2. GalGame 与角色能力
- `GalgamePlayer` 增加 `speaker` 入参，支持使用任务内角色名渲染台词。
- 解决“修改当前角色导致历史任务角色被同步篡改”的问题方向：历史渲染可基于任务自身角色字段而非全局设置。
- 台词解析器增强，清洗 Markdown/HTML 图片标签，减少异常内容注入。

#### 3. 设置中心与 TTS
- TTS 配置增加官方音色预设下拉（CosyVoice 常用音色）。
- 保留自定义 `voice uri` 手动输入能力，支持与官方预设并行。
- “我的音色”与“官方预设”展示逻辑拆分，便于区分来源。

#### 4. 图片与资源路径
- 任务处理时新增 `<img src>` URL 替换能力：
  - 将 `file:///` 等本地路径转换为 `/api/assets?path=...` 可访问路径。
- WorkBoard 渲染层再做一次兜底清洗，避免摘要中裸露本地文件 URI。

### 本轮问题修复记录
- 修复语音开关切换时状态不同步问题（关闭后可立即停止，开启后可重新触发）。
- 修复部分模式页面布局过挤、滚动和容器边界不一致的问题（持续优化中）。
- 修复原文/摘要中图片路径导致展示异常的问题（后端与前端双保险）。

### 当前待继续优化
- 设置模块入口改为“点击导航即弹窗”，移除冗余独立设置页面展示。
- 首页/流程引导进一步减噪与视觉升级（按钮与层级收敛）。
- 上传自定义参考音频后增加“即时试听”能力。
- 原文区滚动限定在内部容器，彻底避免全局页面跟随滚动。
- 历史任务标题规则继续增强（优先提取摘要标题，其次 URL 精简标题）。

### 变更文件（核心）
- `app/page.tsx`
- `app/globals.css`
- `components/InputPanel.tsx`
- `components/GalgamePlayer.tsx`
- `components/WorkBoard.tsx`
- `components/HistoryPanel.tsx`
- `lib/jobs.ts`
- `lib/galgame.ts`
- `lib/types.ts`

### 下一步计划
1. 完成设置入口收敛与弹窗行为统一。
2. 完成历史标题与角色信息持久化（任务级别）。
3. 完成自定义音频上传后的试听与错误提示细化。
4. 对工作台与首页做一轮 UI 精修（去冗余、控密度、提质感）。

---

## 2026-02-22（第二次迭代）

### 新增需求确认
- 设置导航点击后仅弹窗，不再展示单独设置页面。
- 模板工坊改为角色工坊，收敛信息噪声。
- 仅在“角色总结”任务中透出 GalGame 模式；模板总结默认进入分析模式。
- 历史任务需展示更清晰标题，且历史角色不被当前配置污染。
- 上传自定义音频后支持本地试听。
- 原文长文本滚动锁定在内容区，不使用全局页面滚动。

### 本次已落地
- 增加任务级字段 `roleName` 并在提交时写入，用于历史角色固化渲染。
- 历史卡片新增标题提取逻辑（优先摘要标题，回退链接精简）。
- 设置中心音频上传新增 `<audio controls>` 本地试听预览。
- 工作台流程引导补充“进入模式选择”快捷按钮。
- 角色总结模式下输入区提示跳转设置中心，减少重复配置入口。

### 修复中
- 设置页面打开报错：已定位并修复为 Hook 调用顺序问题（条件 return 前后 Hook 不一致），正在回归验证。
- 模式选择逻辑：按 `summaryMode` 动态控制 GalGame 入口，仅角色模式展示。

### 下一步（进行中）
1. 角色工坊视觉重做（卡片化 + 角色包思路）。
2. GalGame 差分/背景/语音入口统一面板化，减少散乱配置。
3. 历史模式二级页面视觉收敛，提升可读性与点击路径清晰度。
